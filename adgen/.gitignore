# Inner package .gitignore — kept in sync with parent /adgen/.gitignore
# This file is redundant when the parent .gitignore is present, but kept
# for safety if the inner package is ever used as a standalone project.
# See ../.gitignore for the canonical list.
.DS_Store
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
.venv/
output/
*.mp4
*.png
*.jpg
.env
models/
comfyui/
